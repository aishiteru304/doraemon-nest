export enum USER_STATUS {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    BOLOCKED = 'blocked',
    BLOCKED_BY_ADMIN = 'blocked_by_admin',
}

export enum ACCOUNT_ROLE {
    ADMIN = 'admin',
    USER = 'user',
}